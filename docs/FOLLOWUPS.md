# Follow-ups

Noticed during the multispace work, deliberately out of scope.
Each becomes its own PR. Nothing here gets fixed on the multispace branch.

| # | Observation | File | Why deferred |
|---|-------------|------|--------------|
| 1 | `envs/cartography_test.yml` leaves `mamba` unpinned. snakemake 7.25.3 invokes `mamba env create`, a CLI mamba 2.x removed, so an unpinned resolve to 2.x breaks `--conda-frontend mamba`. The fix exists on branch `mr/fix-env-drift-and-smoke-test` but has no open upstream PR. | `envs/cartography_test.yml:8` | ~~Pre-existing; belongs to the env-drift PR, not this work.~~ **Deferral expired 2026-08-17.** #109 was the CI-cache PR and never touched this file. Re-verified at `36a38c7`: `envs/cartography_test.yml:8` is still a bare `- mamba`. Still live, and now ownerless. |
| 2 | `ruff` is declared only in `envs/cartography_dev.yml`, so `make lint` cannot run from `cartography_tidy`. Contributors following the tidy-env path get `ruff: command not found`. | `envs/cartography_dev.yml:25`, `Makefile` | ~~Env layout question, orthogonal to multispace.~~ **Deferral expired 2026-08-17.** #104 changed the Makefile, not the env layout. Re-verified at `36a38c7`: `ruff=0.1.6` is declared in `envs/cartography_dev.yml` and nowhere else. Still live, and now ownerless. |
| 3 | README links two files that do not exist: `calculate_convergence.py` and `aggregate_fident.py`. The Snakefile actually calls `calculate_concordance.py` and `aggregate_foldseek_fraction_seq_identity.py`. The README is stale. | `README.md:410,415,416` | Docs-only, upstream's to fix. Tiny separate PR. |
| 4 | `run_foldseek_clustering` creates `temp` before `results` using non-recursive `os.mkdir`, so it raises `FileNotFoundError` for any caller whose results folder does not already exist. Snakemake masks it by pre-creating output dirs. | `foldseek_clustering.py:78-80` | Pre-existing; not on the multispace path. Found while running the function directly in Phase 0. |
| 5 | `str.rstrip(".pdb")` strips a *character set*, not a suffix. Safe for uppercase UniProt accessions, breaks on any protid ending in lowercase `b`/`d`/`p`/`.`. | `foldseek_clustering.py:156-157` | Latent, not currently triggered. Behavior change — needs its own PR. |
| 6 | `aggregate_features.py` uses `DataFrame.add(entry_row)` where a row append is intended. `.add` is element-wise arithmetic with index alignment; it will not append the row and will fill the frame with NaN. Triggers when the override file names a protid absent from the merge. | `aggregate_features.py:83-85` | Real bug, but changing it changes output. Out of scope under I6. |
| 7 | `calculate_concordance.py` writes CSV (no `sep="\t"`) on the empty-input path while the success path writes TSV, and the empty header omits the `concordance_v_*` column. | `calculate_concordance.py:96-99` vs `:115` | Inconsistency in an error path. Separate PR. |
| 8 | The wordwrap post-pass assigns a 1-tuple instead of a function to `apply`, because of a trailing comma inside the parentheses. The `apply` rule is therefore inert for `Protein names`, `Gene Names (primary)`, `Organism`. | `plot_interactive.py:359-364` | Fixing it changes rendered output. Needs its own PR with a visual check. |
| 9 | **`struclusters` is a sequence-identity label presented as structural.** `foldseek clust` is passed the alignment DB with `--similarity-type 2` (= "sequence identity" per the pinned binary's own help) and never sees the TM-score DB. The `StruCluster` column, `plot_similarity_strucluster`, and the `foldseek_clustering.py:51` docstring all imply structure. | `foldseek_clustering.py:48,113-126`; `StruCluster` throughout | Phase 0.5 claim B. Documentation/naming problem independent of this work — **good candidate for a tiny standalone upstream PR**. Inside multispace it becomes `fusable: false` (I2). |
| 10 | `svd_solver="randomized"` with an explicit `random_state` would be both deterministic and O(N²k), where #106's `"full"` is O(N³) and needs the dense matrix in RAM. #106 chose the conservative fix correctly, but the better one exists and matters above N≈5000. | `dim_reduction.py:76` | Performance change to a just-landed determinism fix. Deserves its own PR with timing evidence. |
| 11 | `PC_BLAST_DB` defaults to `refseq_protein` on the `www` path but `nr` on the `remote` path; `PC_BLAST_TIMEOUT` defaults to 1200 vs 900 on those same two paths. | `blast_utils.py:197` vs `:522`; `:185` vs `:523` | ~~Belongs to the BLAST PRs (#107), not here.~~ **Deferral expired 2026-08-17.** #107 rewrote `blast_utils.py` around the divergence and left it in place. Re-verified at `36a38c7` (line numbers updated for the rewrite): the `www` path defaults to `refseq_protein` at `:197` and a 1200s timeout at `:185`, the `remote` path to `nr` at `:522` and 900s at `:523`. Still live, and now ownerless. |
| 12 | `leiden_clustering.py` emits an unpadded `LC0` on the `N<3` path but zero-padded labels otherwise. | `leiden_clustering.py:63` vs `:116-119` | Cosmetic inconsistency in a degenerate path. |
| 13 | #106's `sorted(id_set)` in `aggregate_hits.py` does not survive the round-trip through UniProt: `filter_aggregated_hits` rewrites the list in `uniprot_features.tsv` row order, which is UniProt batch-response order. So the list `download_pdbs` truncates with `[:max_structures]` is not actually sorted, contrary to the in-code comment at `aggregate_hits.py:51-54`. | `filter_aggregated_hits.py:92-93`; `fetch_uniprot_metadata.py:255` | **Not deferred** — commit group 3 fixes this at the truncation point. Listed here so it also reaches upstream. |
| 14 | Retrofit `matrix_io.py` into the five existing raw-matrix consumers (`dim_reduction`, `leiden_clustering`, `get_source_of_hits`, `plot_cluster_similarity` ×2). All five are already label-safe, so this buys assertion coverage rather than a bug fix. | 5 existing files | Deliberately excluded from the multispace PR under I9 — five touched files for no behavior change is a bad review trade. |
| 15 | `BlockStore` has no incremental or partial write: `is_fresh` is one boolean over a whole block, and `Manifest.protids_digest` is order-sensitive, so a cohort file written in a different order forces a full recompute. | `spaces/store.py`, `spaces/manifest.py` | Deferred from Gate B. Real, but it bites the expensive Phase 4 providers (ESM over thousands of proteins), not anything built yet. Needs `read_rows`/`merge_block` plus an order-independent digest for feature blocks. |
| ~~16~~ | ~~`SpaceSpec.weights` is `dict[block_id, scalar]`, so the **per-protein** weights that ADR 0002 names as SNF/`graph` fusion's unique advantage have nowhere to live. ADR 0002 currently overclaims.~~ **RESOLVED 2026-08-17, group 8a.** The observation was right and its framing was the wrong end of the pipe: per-protein weights are an *output*, not a config input, and could not usefully be one -- it would ask the user to know, per protein, which evidence deserves trust. `graph` produces them and `BlockContribution.per_protein_share` records them. ADR 0002 corrected in place; ADR 0013 §5 has the argument. | `spaces/base.py`, ADR 0002 | ~~Deferred from Gate B to Phase 5, where `graph` is implemented. Correct the ADR at the same time.~~ Done. |
| 17 | `metric: "jaccard"` is only reachable on a fixed-width float matrix, so a variable-length domain architecture has to be flattened to a binary indicator that discards order and multiplicity. **Landed as predicted:** `blocks/domains.py` emits a binary presence matrix and declares `euclidean`, because nothing would have honored `jaccard` anyway — see #29. | `spaces/base.py`, `blocks/domains.py` | Deferred from Gate B. Superseded in practice by #29, which is the blocker: a metric-aware reducer has to exist before any block can usefully declare a non-euclidean metric. |
| 18 | Frozen config dataclasses wrap mutable dicts, so `config.spaces["x"] = ...` after construction bypasses validation. | `config_schema.py` | Deferred from Gate B. Configs are built once from YAML; the fix (deep-freezing) is noisy for the risk. |
| 19 | `ProteinCartography.spaces` is shipped in `packages` but is not importable as `ProteinCartography.spaces.store`, because modules use the repo's flat-import convention (`from spaces.base import ...`, enabled by `pythonpath`). | package-wide | Deferred from Gate B. Matches existing repo convention so it is not a regression, but it means the packaging guard checks the manifest entry rather than that the package imports. Fixing it means changing the whole package's import style. |
| 20 | `foldseek search` runs with `--max-seqs` at its default of 1000, which is the direct cause of the 60.5% censoring. Raising it (or using `--exhaustive-search`) would reduce censoring at the source, at O(N²) alignment cost. | `foldseek_clustering.py:87` | Out of scope here — it changes existing output, so it cannot land under the parity requirement. Worth proposing upstream on its own, with timing evidence. |
| 21 | `hit_significance.py` sums `n_queries` across methods, so a hit found by both BLAST and Foldseek can report more "queries" than there are input proteins. Harmless for ranking (the column does not participate in the sort), wrong as a count. | `hit_significance.py:aggregate_significance` | Introduced by group 3. The fix needs a per-method query identity that survives the merge; not worth the complication until something reads the column. |
| 22 | ~~Cluster mode writes no `cohort_report.json`, because it never runs `download_pdbs` and makes no cohort decision. Correct, but the absence currently reads as an oversight rather than a statement.~~ **FIXED in group 9.** | `Snakefile`, docs | Stated in `docs/INTERPRETING.md` §3 under the cohort diagnostic: "Cluster mode writes no cohort report. That is correct rather than missing: it makes no cohort decision, because the user supplies the structures." |
| 23 | `extract_foldseek_hits.py` truncates with `hits[:max_num_hits]` after `unique()` across concatenated per-database frames, so `max_foldseek_hits` keeps the first N in *file order*, not the N most significant. The e-values needed to rank properly are right there in the frame. | `extract_foldseek_hits.py:96-98` | Pre-existing, and changing it changes which hits reach the map — cannot land under the parity requirement. Same family of defect as ADR 0008, one stage upstream. Worth its own PR. |
| 24 | `extract_foldseek_hits.py` calls `pd.read_csv` *before* its `os.path.getsize(file) == 0` guard, so a genuinely empty `.m8` raises `EmptyDataError` and the guard is unreachable. | `extract_foldseek_hits.py:62-65` | Pre-existing dead code. Noticed while building `hit_significance.py`, which handles the same case in the other order. |
| 25 | **`extract_foldseek_hits.py` returns nothing under `--mode tmalign`.** In that mode the web API puts a TM-score in the column `constants.FOLDSEEK_COLUMN_NAMES` calls `evalue`, so the default filter `evalue < 0.01` keeps 0 of 938 hits on a live query. With a relaxed threshold it would keep the *least* similar structures. Same 21 columns, same positions, different meanings, and the mode is recorded nowhere. | `extract_foldseek_hits.py:75`; `constants.py:5-27` | Latent: the Snakefile never passes `--mode`, so the pipeline always runs 3diaa. Reachable by calling the script directly. Real fix is to record the mode alongside the results. |
| 26 | `foldseek_apiquery.py` advertises `--mode tmalign` but the Snakefile hardcodes the call without it, so the mode is unreachable through the pipeline. Either expose it (and handle #25) or say in the script's help that the pipeline does not use it. | `Snakefile` `run_foldseek`; `foldseek_apiquery.py:52-56` | Exposing it changes which hits return, hence the cohort, hence the map — cannot land under the parity requirement. |
| 27 | `compute_block`'s freshness check builds its `expected` manifest with `protids=[]` and no `inputs`, so its `cache_key` can never match the written one and `is_fresh` is always False -- every block recomputes on every invocation snakemake allows through. Harmless today (snakemake's own file-timestamp logic does the real skipping) and wasteful once the Phase 4 providers are expensive. | `compute_block.py`; `spaces/store.py` | Related to FOLLOWUPS #15. The written manifest now carries inputs (fixed in `0fff979`), so the remaining half is making the *expected* manifest describe the same thing. |
| 28 | `blocks/threedi.py` builds its k-mer vocabulary from the observed cohort, so two runs over different cohorts produce feature matrices whose columns are not comparable. Correct for a single run and recorded in the manifest, but it blocks any cross-run comparison of 3Di blocks. | `blocks/threedi.py` | `kmer_profile` already accepts an explicit `vocabulary=`; what is missing is a way to pin one from config. Wait until something actually needs cross-run comparison. |
| 29 | **`reduce_space` never consults `spec.metric`.** It feeds `block.features` straight into a euclidean PCA, so a block declaring `cosine` or `jaccard` would have that metric recorded in its manifest and silently ignored by the only thing that reduces it. `blocks/domains.py` refuses `jaccard` rather than declaring it, which keeps the manifest honest but leaves the right distance for set data unavailable. | `reduce_space.py:_features_for`; `spaces/reducers/core.py` | Needs a metric-aware reducer path — precompute the distance matrix for non-euclidean metrics and feed a `metric='precomputed'` reducer. Same missing capability that makes `reduce_space` refuse `kind: pairwise` blocks today, so the two should be fixed together. |
| 30 | ~~`biophys` and `domains` take their protid set from the features table, while `tmscore` takes it from the similarity matrix and `threedi` from the descriptor file. Nothing checks that the four agree.~~ **FIXED in group 7**, not deferred. | `coregistration.py` | `shared_index` intersects every compared space's protids in a named reference space's order and enumerates -- not counts -- every protein each space lost reaching it. Intersecting rather than refusing is deliberate; see ADR 0011 §1. |
| 31 | The `domains` k-mer-equivalent — its family vocabulary — is cohort-scoped, so two runs over different cohorts produce feature matrices whose columns are not comparable. Identical in shape to #28 for `threedi`. | `blocks/domains.py` | `domain_matrix` already accepts an explicit `vocabulary=`; as with #28, what is missing is a way to pin one from config. Fix both at once. |
| 32 | **`spec.normalization` is recorded on every block and applied nowhere.** `reduce_space` feeds `block.features` straight into PCA, so `zscore_within` and `unit_mean_distance` are declared in every manifest on disk and honored by nothing. Exactly the shape of #29 for `spec.metric`, found the same way. It matters most for `biophys`, whose four columns are a pH beside a per-residue charge: unnormalized, the euclidean distance between two of its rows is the isoelectric point and almost nothing else. | `reduce_space.py`; `spaces/reducers/core.py` | Applying it would change the default map, so it cannot land under the byte-identical requirement without a decision about what the default should be. Fix with #29 -- the same reducer rewrite has to touch both, and ADR 0011 §4 records that co-registration deliberately shares the defect rather than describing a geometry no map is drawn from. |
| 33 | ~~**Committed files reference five documents the PR will not contain.**~~ **DECIDED 2026-08-18 and executed.** `docs/FOLLOWUPS.md` — this file — now ships with the PR: it is a normal artifact for work this size and is the evidence that problems were noticed and deferred deliberately rather than missed. `docs/REVIEW_LOG.md` does **not**: at 2,323 lines it was 52% of all 107 references, is unlinked from the README, describes how the work was produced rather than what the code does, and is the one document nobody would ever update. It is preserved in full in the private notes repo. `PLAN.md`, `CLAUDE.md`, `docs/EXPLORATION.md` and `docs/PR_NARRATIVE.md` stay local — they are working documents about how the work was run. | committed docs and code, repo-wide | **Closed.** All 36 references that would have dangled are rewritten to carry their own reason rather than a pointer; the `FOLLOWUPS #N` references that remain now resolve, because this file ships. Verified by grep: zero references to any non-shipping document survive in `git ls-files`. |
| 34 | **`remove_nans` substitutes a synthetic zero for a cluster with no data.** Its `default=(0,)` fires whenever a cluster's values for a column are all NaN, so `plot_cluster_distributions` then runs a Mann-Whitney of `[0.0]` against every other cluster's real distribution and marks the result on the plot. A cluster whose structures all failed to download therefore reads as "significantly lower pLDDT" rather than as "no measurement". Same shape as #29/#32: a value that is present, looks like evidence, and is not. | `plot_cluster_distributions.py:71-81`, called at `:215` | Pre-existing, and fixing it changes a shipped SVG, so it cannot land under the parity requirement. `enrichment.py` takes the other branch deliberately — an all-missing group is reported `untested` with the reason, never compared against an invented value — and ADR 0012 records why. Worth its own upstream PR. |
| 35 | **EC number and subcellular localization are never fetched, so two of the four enrichment categories PLAN Phase 6 names have no data source.** `fetch_uniprot_metadata.REQUIRED_FIELDS_DICT`/`OTHER_FIELDS_DICT` request neither `ec` nor `cc_subcellular_location`. Taxon (`Lineage`) and domain architecture (`Pfam`/`InterPro`) are present and are what the demo enriches on. | `fetch_uniprot_metadata.py:27-51` | Adding a field changes the columns of `uniprot_features.tsv` and therefore of `aggregated_features.tsv`, which is exactly what the byte-identical guarantee forbids. `enrichment` is column-driven and names any requested-but-absent column in its report rather than skipping it silently, so the two categories arrive the moment the columns do. |
| 36 | **Every `DiagnosticsConfig` field is validated and read by nothing.** `bootstrap_replicates`, `subsample_fraction`, `leiden_resolution_sweep` and `negative_controls` are parsed, range-checked, and consulted by zero files outside `config_schema.py` and its test. A config can set `bootstrap_replicates: 500` today and get exactly the same run. Fourth instance of the pattern behind #29 (`spec.metric`), #32 (`spec.normalization`) and the unreachable provider defaults the review gates found — a value that is present, validated, and therefore looks honored. | `config_schema.py` `DiagnosticsConfig`; nothing else | ~~Not deferred past group 8.~~ **Half discharged 2026-08-17 (group 8b), and the remainder is now a check rather than a note.** `DiagnosticsConfig` gained `k`, which `diagnose_space` reads and which `test_diagnostics_config.py` proves reaches the statistic — two runs differing only in `diagnostics.k` produce different numbers, so it is live rather than merely round-tripping. The original four are still dead and are now listed in that file's `NOT_YET_CONSUMED` table against the Phase 5 item that will read each: `bootstrap_replicates` and `subsample_fraction` → item 3 (group 8c), `leiden_resolution_sweep` → item 7 and `negative_controls` → item 8, both blocked on per-space clustering. The test fails in **both** directions, so the table shrinks when 8c lands instead of going stale. Writing it immediately found #39. |
| 37 | **A pairwise block cannot enter a fused space, and `late` is its natural home.** `reduce_space` refuses any block that supplies `distances` rather than `features`, with the message "use representation: profile". That was right when a space was one block reduced by PCA; under `late` it is not, because `late` computes a distance matrix per block anyway and a pairwise block arrives with one already. Supporting it means handling the condensed upper triangle and deciding what `early` should do with a block that has no feature space. | `reduce_space.read_blocks`, `fusion.fuse_late` | Deferred from group 8a. Nothing currently produces a pairwise block -- `tmscore` declares `representation: profile` and the other three are feature blocks -- so this is capability for a provider that does not exist yet. |
| 38 | **`coregister.py` rebuilds every compared space's geometry, so a fused space is fused twice per run.** What `reduce_space` writes to disk is the 2-D embedding, and two of the three co-registration metrics need the full-dimensional matrix, so `coregister` calls `features_for` again for each compared space. Deterministic, so it is the same answer; but for a `graph` space it re-runs the whole SNF diffusion, and at N=2500 with several blocks that is not free. | `coregister.py:main`, `reduce_space.features_for` | Deferred from group 8a. The fix is to persist the pre-reduction matrix beside the embedding, which is an on-disk format change and a new cache-invalidation question. Not worth it until fusion is the expensive step; it is not, at demo scale. |
| 39 | **`from_legacy` dropped the entire `diagnostics:` key on the legacy branch, and the comment three lines above says why that must not happen.** The legacy branch — taken by any config without `blocks`/`spaces`, i.e. every plain cluster-mode run — builds its `MultispaceConfig` from a literal dict that carried `cohort` and `enrichment` but not `diagnostics`. A cluster-mode config could set `diagnostics.k` and be silently ignored, or misspell a diagnostics key and never reach `_reject_unknown_keys`. Directly above the omission sits a comment explaining that `enrichment` is carried through *precisely* to prevent this, naming it "the exact failure `_reject_unknown_keys` exists to prevent one level up". | `config_schema.py` `from_legacy` | **Fixed in group 8b**, in the commit that found it. Recorded rather than dropped because the lesson generalizes and is now the second instance: a comment that states an invariant does not enforce it. Found by the consumption test #36 demanded, within minutes of that test existing — the same way ADR 0013 §6's `STRATEGY_PARAMS` found a dead `params: {K: 20}` within seconds of existing. |
| 40 | **Phase 5 item 6 was recorded as unbuilt in PLAN and is fully built.** `cohort.CohortReport` already records candidates before filtering, candidates before and after truncation, the selection rule, whether truncation was reproducible, and the retained-versus-discarded taxonomic comparison — which is item 6's complete text — and `download_pdbs` declares `cohort_report.json` as a rule output. Nothing was missing but a reader. | `cohort.py` `CohortReport`; `Snakefile` `download_pdbs` | **Not a defect; a planning error, and worth keeping as one.** It was caught only because group 8b checked the plan's claim against the code before implementing against it. The near miss was a second implementation of a report that already existed. Group 8b wires the existing file into the space report instead, and only in search mode — cluster mode makes no cohort decision, so the absence there is correct rather than a gap. |
| 41 | **Cross-cluster edge retention uses the legacy Leiden partition, which is a partition of the `structure` space, for every space.** `diagnose_space --clusters` is wired to `leiden_features.tsv`, the pipeline's only clustering. For the `structure` space that is the right partition; for `physicochemistry` or `families` it is a structural partition being used to ask a censoring question about a non-structural block, and the number is still well defined but is answering about the wrong grouping. | `Snakefile` `diagnose_space`; `diagnose_space.py` `--clusters` | Deferred to group 8c, which has to answer the per-space clustering question anyway for Phase 5 items 7 and 8. Same shape as ADR 0012 §1, where the enrichment table describes the `structure` space and says so in every row. The censoring section should say so too until spaces cluster in their own right. |
| 42 | **The Leiden partition is not reproducible across conda environments, and at production scale the disagreement is large.** Originally found on the 11-protein demo, where two environments agreeing on scanpy 1.9.3, leidenalg 0.9.1, igraph 0.11.9, numpy 1.23.5 and scikit-learn 1.2.2, and differing only in **scipy 1.13.1 against 1.15.2**, returned different two-cluster memberships. `sc.tl.pca(svd_solver="arpack")` goes through scipy's sparse eigensolver, and on an 11x11 matrix with `n_neighbors` clamped to 10 of 11 the graph is nearly complete and Leiden's optimum is degenerate, so a tiny change in the principal components decides the tie. **The original entry then said "At N=250 the two environments agree exactly", and treated that as bounding the problem. It does not.** Re-measured 2026-08-18 on a real 2530-protein production matrix: `leiden_clustering.py`, same script and same input file, produced **6 clusters** in the archived Modal run and **18** when re-run locally, with **ARI 0.436** between the two partitions. Scored against MEROPS S1 subfamily labels at matched cluster count the archived partition recovers ARI 0.063 where the local one recovers 0.171, a factor of 2.7. **That factor is n=1 and does not generalise.** Repeated on a 2465-protein actin production run with the identical procedure -- same scorer, same 200-draw permutation control, same seed, same class floor, and a local resolution chosen only to reproduce the archived partition's cluster count -- the two environments returned ARI 0.2270 and 0.2288 against Pfam family, a ratio of 1.008, and 0.1135 against 0.1130 against taxonomic supergroup. So the environments disagree materially on *both* families at default settings (6 clusters against 18 on chymotrypsin, 6 against 14 on actin) but the cost in recovered signal was large on one and nil on the other. Quote the disagreement, not the factor. Both environments were built from the same unpinned `envs/analysis.yml`; the exact version delta is not recoverable from the run log, so scipy is the leading hypothesis rather than a proven cause. What *is* proven is that the unpinned file permits materially different shipped output at realistic cohort size. **Reproduced at production scale on the actin cohort, 2026-08-19, and this is now the strongest evidence for the entry.** Same 2,465-protein TM matrix, same `leiden_clustering.py`, same scanpy 1.9.3 / leidenalg 0.9.1 / igraph 0.11.9: **python 3.11 + scipy 1.15.2 returns 6 clusters; python 3.9 + scipy 1.13.1 returns 14**. Each is bit-identical on a rerun, and the 14-cluster partition matches `legacy_leiden_local.tsv` protein-for-protein -- i.e. it is the partition the published actin map's LC00-LC13 labels come from. So a reader comparing the published map against one rebuilt today sees a different number of clusters for reasons that have nothing to do with the data, the cohort size, or any parameter. Everything downstream of the partition inherits this: cluster enrichment, the negative-control margins, and every `partition` verdict in the explorer. | `leiden_clustering.py`; `clustering.py`; `envs/analysis.yml` | Pre-existing and not introduced by this work -- group 8c only made it visible, and the production-scale measurement above only sharpens it. `envs/analysis.yml` does not pin scipy at all; it arrives transitively, the same drift shape ADR 0006's context section describes for matplotlib and setuptools. Two candidate fixes, and the first is now clearly worth doing on its own: pin scipy in `envs/analysis.yml`, or pass `svd_solver="full"` below some N as `spaces/reducers/core.py` already does for a related reason (ADR 0004). The second is a behavior change to a shipped output and cannot land under the parity requirement. **Note the parity harness cannot see any of this**: it runs both sides of its comparison in one environment (PLAN section 0.1). |
| 43 | **A neighborhood diagnostic cannot be trusted when `k` approaches the cohort.** `diagnostics.stability` clamps `k` to what a subsample can supply and then reports a value; at the demo's eleven proteins that clamp lands on 8 of 9, so every protein's k nearest are all the others and the Jaccard is 1.0 by construction. The same pressure exists for trustworthiness and continuity, where the demo's `k` is 6 of 11 -- over half the cohort -- and `embedding.py` reports the number without qualifying it. | `diagnostics/embedding.py`; `diagnostics/stability.py` | **Half fixed in group 8c**: `stability` gained `informative` and a warning naming the fraction, and the demo now reports `informative: false` for all seven spaces. `embedding` was left alone deliberately -- it has a hard validity ceiling at `k < (2N-1)/3` and clamps to it, but nothing between that ceiling and "local" is flagged, and adding a band there is a change to a shipped diagnostic rather than to a new one. Worth doing in group 9 alongside the explorer, which has to decide what to grey out. |
| 44 | **The mutation harness cannot see anything a test-file change does, and this work cited it as if it could.** `mutation_check.py` never runs pytest -- it runs pipeline, reducer and component steps and diffs output trees. All 17 mutations name six files: `dim_reduction.py`, `foldseek_clustering.py`, `leiden_clustering.py`, `aggregate_features.py`, `cohort.py`, `spaces/reducers/core.py`. **Zero mutants touch `fusion.py`, `diagnose_space.py`, `enrich_clusters.py` or `diagnostics/*`** -- exactly the four modules whose test files the speedup consolidated. An identical `12 detected / 5 survived / 0 unexplained holes` before and after was therefore guaranteed by construction, and `f206875`'s claim that "the mutation harness is the evidence rather than the claim" was wrong. The proof is Gate E finding 1: the harness reported zero holes while a real hole was open. | `ProteinCartography/tests/mutation_check.py` | **Not a defect in the harness; a defect in how it was cited.** It measures whether the *parity test* notices a change to pipeline code, and it does that correctly. The fix is either to stop invoking it as evidence for test-file changes -- done, in the commit that records this -- or to add mutants in the four diagnostic modules. The second is the better long-term answer and is a real piece of work: those modules are additive output the baseline never produces, so §0.1's fourth cause applies and every such mutation would survive. It needs a different harness, comparing HEAD against HEAD with the mutation applied, not HEAD against the baseline. |
| 45 | **The only detector for a never-completing Foldseek ticket was wall clock, and the speedup removed it.** With `mocks.py` altered to return `RUNNING` forever, `parity.run_pipeline` now returns cleanly in 52 s and the new sleep guard is satisfied (`run_foldseek` 3.74 s, ceiling 15 s). Before, the same regression cost 60 polls x 30 s per pipeline -- about two hours across the four-run fixture, which nobody misses. The pipeline's *output* was always insensitive to it, so nothing else was watching. The new guard is one-sided by construction: it detects "the hook failed to load" and cannot detect "the hook loaded and the loop spun 60 times". | `ProteinCartography/tests/parity.py`; `ProteinCartography/mocks.py` | Deferred, with the shape of the fix recorded: make the guard two-sided by counting poll iterations rather than measuring seconds -- the hook is the natural place to count, since it already intercepts every sleep. **And the related upstream defect matters more:** `foldseek_apiquery.py` tests `if elapsed > FOLDSEEK_SERVER_TIMEOUT` with `elapsed` reaching exactly 1800, so a ticket that never completes falls out of the loop and **downloads anyway with no error**. That is in `upstream/main`, is independent of this work, and is a good standalone PR -- the wall-clock hang was masking it. |
| 46 | **`spec.version` is recorded on every block and invalidates nothing, and its own docstring said it did.** `BlockSpec.version` is documented as "bumped by a provider when its output changes meaning, so that cached blocks are invalidated", and `docs/EXTENDING.md` said "it is part of the cache key, so cached blocks from an older version invalidate rather than being silently reused". It is not. `spec.version` lands in `Manifest.derived` (`store.py:264`), and `Manifest.cache_key` excludes `derived` deliberately -- a key folding in output-derived fields could only be computed by someone who had already produced the output. Confirmed empirically by the Gate E battery: write a block, bump the provider version, ask `is_fresh` with everything else identical, get **True**. Fourth instance of #29/#32/G6.4. | `spaces/base.py:138`; `spaces/manifest.py:195-219`; `spaces/store.py:264` | **Both docstrings corrected in the Gate E fixes (`0e2dc37`); the code fix is deferred with a reason.** Making it honest means adding a `provider_version` field to `Manifest`, folding it into `cache_key`, and passing it at eleven `Manifest.build` call sites -- four of which build the manifest before the `BlockSpec` exists. That changes the cache identity of every block on disk, which is not a thing to land in the same session as a parity re-run and a checkpoint fix. Note it interacts with #27: `compute_block`'s `expected` manifest omits the field too, so until #27 is fixed the freshness check is always-stale for a different reason and this change cannot be observed through the pipeline. Fix the two together. |
| 47 | **Adding an output to a snakemake *checkpoint* is not additive, and nothing in the repo says so.** GE.2 cost a silent wrong answer: an output that no job requests is never a reason to re-run a checkpoint, so on a pre-existing tree the checkpoint stays un-run, `checkpoints.<name>.get()` raises, every job depending on that resolution vanishes from the DAG, and the run *succeeds* on an incomplete input set. Fixed for the two instances that existed by declaring each output only when its consumer is in the DAG, and `test_review_regressions.py` locks those two -- but the rule is general and the check is not. | `Snakefile` `download_pdbs`; `ProteinCartography/tests/test_review_regressions.py` | The durable fix is a test that walks every rule's declared outputs and fails when a checkpoint declares one that no other rule consumes and `rule all` does not request. It needs the resolved DAG rather than the source text, so it wants snakemake's API rather than a grep, and it should run for the legacy, significance and spaces configs. Worth doing: this failure mode is invisible to the rule count, invisible to parity, and produces no error. |
| 48 | **The Snakefile still spells out six names that `spaces/layout.py` now owns.** `summary.tsv`, `diagnostics.json`, `cohort_report.json`, `cluster_enrichment.tsv`, `hit_significance.tsv` and `_explorer.html` each appear twice in the Snakefile, and the first four are also defined in `spaces/layout.py`, which the writers and the explorer now share. The Snakefile can import it -- it already does `sys.path.insert(...)` then `import config_schema` -- so closing the loop is mechanical. Not done here because these declarations are on the default path and inside the one file every reviewer reads line by line: the duplication is local and visible, unlike the cross-module kind that produced four silent explorer defects. The cost of getting it wrong is a changed output path and a broken parity run; the benefit is uniformity. Worth doing, but not inside this PR. | `Snakefile`; `ProteinCartography/spaces/layout.py` | Deferred deliberately. The cross-module drift, which was the one failing silently, is fixed and has an end-to-end guard (`test_output_layout.py::test_coregister_writes_where_the_explorer_reads`). What remains is within-file duplication that a reviewer can see. |
| 49 | **`diagnostics/metricity.py` reports a number and sets no threshold, and the evidence for keeping it that way is now four cohorts rather than one.** Fully dense cohorts measured to date: 44.6% (160-protein actin, `P60709`), 36.5-53.2% (cores of 190-339 from a 2530-protein S1 peptidase run, `P07338`), 59.3-64.5% (cores of 99-119 from a 2174-protein run of the *same query*). Every one is far from the sub-`1e-9` a genuinely Euclidean matrix returns, so `1 - TM` is robustly non-Euclidean -- but the value spans 36-65%, and the widest gap is between two runs of one query rather than between families. Cohort composition moves it further than family identity does, which the 160-protein cohort also showed internally (15.3% to 49.3% across its sub-cohorts). A threshold fitted to any one of these would be a constant derived from a cohort. **Re-measured 2026-08-19 at a matched core size, which the figures above are not**: at n=100 the same three cohorts give 31.8% (2530 chymotrypsin), 57.5% (2174 chymotrypsin) and 79.1% (2465 actin), and the fraction climbs with core size inside each one -- actin runs 79.1% / 89.4% / 91.1% / 92.9% at n=100/200/400/600. So a quoted figure needs its core size as well as its cohort, and two of the three ranges above were taken at sizes that differ by a factor of three. **Repeated runs are not a source of variation at all**: three archived runs of the identical 2174-protein query differ in 0 of 4,726,276 cells once the #106 column permutation is repaired, so the archive holds three distinct cohorts rather than five. | `ProteinCartography/diagnostics/metricity.py` | Deferred deliberately, and the module says so in its output: `verdict` is `None` and `verdict_note` names this entry. Setting a threshold needs a rule for what the cohort has to look like before the number means anything -- size, divergence span, censoring -- not simply a second family. Justify it in an ADR from that rule, not from a percentage. |
| 50 | **Nothing consumes a pairwise block, so a `representation: direct` block is computable and unusable.** `blocks/tmscore.py` produces `kind="pairwise"` distances; `reduce_space.read_blocks` raises `SystemExit` for any block whose `features` is `None`, saying a precomputed distance needs a metric-aware reducer. The store round-trips it and nothing else reads it. So the config gate, the symmetrization rule, the censoring channel and now the metricity report are all computed on a path that terminates at the store. | `ProteinCartography/reduce_space.py`; `ProteinCartography/blocks/tmscore.py` | Not introduced here and not a defect -- `direct` was always gated behind an explicit opt-in and the refusal is honest and specific. Recorded because it locates the metricity report: it is attached to the *block* rather than to the space manifest precisely because the space-level consumption site does not exist yet. When a metric-aware reducer lands, the number is already on the input it describes. |
| 51 | **A single metricity figure on a censored matrix is roughly half sentinel, and the report cannot say which half.** Measured on the archive's two largest production matrices (S1 peptidase cohorts, query `P07338`, N=2530 and N=2174): the whole-matrix negative-mass fraction is 88.2% and 89.0%, because censored pairs are read as distance 1.0 and 60.5%/54.0% of cells are that sentinel. Fully dense cores of the *same* matrices return 36.5-53.2% and 59.3-64.5%. `metricity_report` records `cohort.censoring_rate` beside the number, which is what lets a reader tell the two regimes apart, but it does not compute the decomposition. | `ProteinCartography/diagnostics/metricity.py` | Deferred because the companion measurement needs a way to *choose* a dense core, and every obvious rule is a threshold or a heuristic on cohort size -- the thing #49 says not to fit from one family. The measurement itself is cheap once the rule is agreed: take the mutually-measured submatrix, refine to full density, report both figures with their densities and their minimum TM. Worth doing before anyone quotes a production metricity number as a property of TM-score. |
| 52 | **The sub-threshold coverage diagnostic is degenerate on the matrices the pipeline actually produces.** On measured pairs of two production runs, the fraction below TM 0.17 is 0.00% (N=2530) and 0.01% (N=2174), against 12.9% on the dense 160-protein cohort. The cause is structural rather than a bug: `--max-seqs` keeps each query's *best* 1000 hits, so the weakest pairs are precisely the censored ones and any statistic computed on measured pairs is truncated from below by construction. Computed the other way, with censored zeros left in, it returns 64.2% -- which is the censoring rate under another name. | `ProteinCartography/matrix_io.py` `summarize_censoring`; PLAN Phase 9 item 4 | Recorded rather than fixed, because there is nothing to fix in the code: the diagnostic is correct and the cohort is what makes it uninformative. The consequence is for Phase 9, which planned to use the sub-threshold fraction as a coverage diagnostic and to exclude pairs from rate fitting on it. That plan only works on a dense matrix. On censored output the honest statement is the censoring rate itself plus the per-protein distribution, both of which `summarize_censoring` already returns. |
| 53 | **Cluster mode refuses fewer than 10 PDB files, and the number is admitted to be arbitrary.** `config_utils.py` raises when an input directory holds fewer than `min_num_pdb_files`, above a `# TODO (KC): decide on a less arbitrary minimum`. Nothing downstream has a 10-structure floor: the components with real floors all use N<3 and degrade on their own terms (`leiden_clustering.py`, `diagnostics/redundancy.py`, `coregister.py`). The shipped demo sits at 11, one file from unusable, and there is no override. | `ProteinCartography/config_utils.py` | Upstream's file and upstream's number, so it stays out of this PR's diff. The fix is either a stderr caveat instead of a raise, or lowering the floor to 3 to match what the code actually requires. Worth proposing on its own, with the observation that four separate components already choose 3. |
| 54 | **Roughly 260 test assertions are coupled to English prose, serializer whitespace or rng draw order rather than to behaviour.** Examples: `pytest.raises(IndexAlignmentError, match="discard the rest")` where the exception type is already the discriminating fact; an assertion embedding the count `"3 proteins are missing"` that has already grown an `or` clause to absorb one rewording; `"realized_share": 0.733` pinning `json.dumps` separator defaults; `className = "shares"` pinning rendered JS whitespace; `len(caught) == 1` on a warnings list that any new pandas DeprecationWarning inside `read_csv` will break. | `ProteinCartography/tests/` broadly | Deferred as a sweep rather than fixed piecemeal, because the fix is one pattern applied ~260 times: keep one discriminating token per assertion, drop the remediation prose, and parse the embedded payload rather than grepping rendered output. **One instance is not merely brittle and should be fixed first:** an assertion in `test_parity.py` that a reword converts into a silent *pass* rather than a failure. |
| ~~55~~ | ~~**Several absolute snapshot counts are asserted where the quantity is derivable.** The CI DAG-count check has already broken once for a reason unrelated to what it tests (upstream added a rule), and this branch rebases onto `upstream/main` weekly, so it fires on upstream's schedule rather than on a defect. Others: `n_compared == 3` fails on any additive output while the reduction is byte-identical; the demo's protein count is spelled out independently in more than one file; an exact header-equality check fails on an *additive* column, which is the change type this branch prefers.~~ **RESOLVED at commit `dfb3b97`**, and re-verified against HEAD on 2026-08-19 because this entry still read as deferred six commits later. All four are derived now: the CI check resolves the same dry run on the fork point and on HEAD and requires them to agree, rather than hardcoding 17 and 26; `n_compared` is a floor (`>= 35`, `>= 3`) with the artifacts that carry the claim named individually, so an additive output cannot break it and an empty comparison cannot satisfy it; the protein count is `config["max_structures"] + len(list(input_dirpath.glob("*.pdb")))`; and the header check is `{"evalue", "bits", "n_queries", "sources"} <= set(columns)` plus a fixed index column. | `.github/workflows/multispace.yml`; `ProteinCartography/tests/test_parity.py`; `ProteinCartography/tests/test_pipeline_in_search_mode.py` | ~~Deferred; the fix is to derive each expectation rather than hardcode it.~~ Done. Recorded rather than deleted because the entry outlived its fix and was queued for a second implementation -- the same near-miss as #40, where PLAN listed a built feature as unbuilt. One correction to the original text: the protein count was spelled out in two files, not three. |
| 56 | **The metricity fraction is not trustworthy on a saturated cohort, the module cannot tell you when that is, and the check that was going to fix it has now been refuted too.** The original reading: on fully dense cores, a 2465-protein actin run returns 85-93% where a 2530-protein S1 peptidase run returns 36-53%, and against a shuffled control of its own distances actin scored 0.87-0.94 where chymotrypsin scored 0.41-0.58. **A second S1 peptidase cohort -- same query, `P07338` -- lands at 0.748, in the middle of that gap** (measured 2026-08-19 at a matched core of n=100, eight seeded draws each: 0.376 for the 2530 cohort, 0.748 for the 2174 cohort, 0.834 for actin). **The "factor of 4.3 the wrong way" this entry used to report here is RETRACTED**, for two independent reasons found on 2026-08-19. It compared the LARGEST within-family gap (A1 vs A2, 0.372) against the SMALLEST between-family gap (A2 vs B, 0.087) while not quoting A1 vs B, which is **0.458 -- the largest gap in the whole dataset, and a between-family pair**. Max-within against min-between is not a comparison. And the ranking it rests on is not stable: under uniform sampling with sentinel fill the actin cohort goes from the highest ratio to the lowest, on identical TM-scores, because 60-75% of cells are decided by the fill rule. What survives is only that the draw ranges overlap: family A spans 0.325-0.770, actin 0.653-0.973. Two further properties make the figure unsafe to compare at all. It **rises monotonically with core size** (actin 0.834 -> 0.922 -> 0.929 -> 0.943 at n=100/200/400/600), so any comparison at unmatched n is confounded the way an unmatched cluster count is. And it **does not order cohorts by saturation**: the 2530 cohort's core sits at median TM 0.9525 and returns the *lowest* ratio of the three, below the 2174 cohort's core at median TM 0.8254. | `ProteinCartography/diagnostics/metricity.py` | **Still deferred, and now for a stronger reason: three substitutes have been refuted, including the one this entry previously called the only working signal.** (a) The spectrum's *dynamic range* is the sign of a numerical zero -- double-centring guarantees one mathematically zero eigenvalue, returned as +4e-20 for actin (so it becomes the minimum positive) and -2e-17 for chymotrypsin (so it is excluded); excluding the nullspace properly leaves a factor of five that overlaps across core sizes. (b) "What fraction of the negative mass sits above an n*eps*max noise floor" returns 100.0% on every core of both families. (c) The shuffled-control ratio separated two cohorts and stopped separating them the moment a third was measured -- the same failure as (a) and (b), found the same way. **It was not shipped, deliberately.** Reporting a number whose only stated purpose is detecting saturation, when it has just been shown not to track saturation, invites precisely the cross-cohort comparison that is invalid; that is worse than reporting nothing. What a real guard needs is a rule for when a cohort is comparable at all -- core size, divergence span, censoring -- which is the same thing #49 says a threshold needs, and there is now a mechanism for why. The two S1 peptidase cohorts are **nested**: all 2174 members of the smaller are in the larger, which has 356 more. Their fully dense cores nevertheless share only **43 proteins, 36% of the smaller core** (339 against 119). The per-query cap is what does it -- Foldseek reports about the top 1000 partners per query, so enlarging the pool changes who measured whom, and the mutually-measured subset lands on different proteins. Cohort size therefore decides which proteins are comparable at all, upstream of any statistic computed on them. That is why matching core *size* is necessary and not sufficient: two cores of 100 drawn from nested cohorts of one query are still mostly different proteins. **And a direct test shows the selection is the WHOLE effect.** The two cohorts contain identical data -- all 1,606,411 pairs measured in both runs have bit-identical TM values, max difference exactly 0. Restricted to 100 proteins fully measured in both matrices, the two give the same answer to every digit: metricity 38.3%, shuffled-control ratio 0.484, median TM 0.9064. So the 0.376-against-0.748 gap is **entirely an artifact of which subset the dense-core procedure selected**, and nothing about the cohorts, the runs or the biology. Within one core, twelve random draws span only 0.349-0.383 and correlate with neither median TM (+0.02) nor TM spread (+0.04), so this is systematic in the core chosen, not sampling noise. The statistic is therefore not a property of a cohort at all; it is a property of an arbitrary analyst choice, which is a stronger reason not to threshold it than the one originally given. Until then: **do not compare a metricity figure across cohorts**, matched core size or not, and do not quote the actin number. |
| 57 | **Three tests are most of `make test`, and none of them is this branch's.** `test_pipeline_in_search_mode_domain.py::test_pipeline_in_search_mode_domain_map` runs 59.0s of a 73.3s suite *on this branch*; it arrived with upstream #110, is unmodified here, and carries no marker, so `make test` (which is `pytest -vv -s .`) runs it every time. This is worth writing down because this branch adds roughly 1,200 tests and a reviewer timing the suite will reasonably attribute its cost to them: the other ~1,209 tests together take 14 seconds. **On `upstream/main` the profile is completely different and much worse, so neither figure transfers.** Measured 2026-08-19 in a worktree at `cd4bee9`: `test_pipeline_in_search_mode_with_mocked_api_calls` alone is **648.0s**, and the domain test that dominates this branch's suite is only 55.2s there -- the two together are 703.6s. The same command with the three end-to-end tests deselected is **61 tests in 0.56s**. The reason the branch figure is not upstream's is that `test_pipeline_in_search_mode.py`'s Foldseek sleep is gated on mocks *here* and not there; that gating is one of this branch's twelve touched files, and it is what turns a 648s test into a fast one. The third test, `test_pipeline_in_cluster_mode.py`, polls the **live public Foldseek server**, so its duration is not under the repository's control at all and a contributor with no network cannot run the default suite to completion. | `ProteinCartography/tests/test_pipeline_in_search_mode_domain.py`; `test_pipeline_in_cluster_mode.py`; `test_pipeline_in_search_mode.py` | Not ours to change, and changing it here would be a drive-by edit to upstream files. **A candidate branch is prepared and pushed to the fork as `mr/mark-pipeline-tests-slow`, off `cd4bee9`, and is deliberately NOT opened anywhere** -- it changes what upstream's default `make test` runs, so it is upstream's call. One correction to this entry's earlier text, found while preparing it: it described the change as one line using "the `slow` marker this repo already defines in `pyproject.toml`". **Upstream defines neither the marker nor `--runslow`; both are this branch's.** So the branch registers `slow` and excludes it through `addopts`, which is exactly the convention upstream already uses for `live_ted`, rather than importing this branch's custom flag and conftest hook. Four files, five added lines, and `pytest -m slow` still runs them. |
| 58 | **Four of five spaces are banded "this map should not be read as a finding" on both cohorts measured, and for at least one space per cohort an external check says otherwise.** On a 2465-protein actin run: `structure`, `local_structure` and `fused_late` trip the neighbourhood clause (mean stability 0.264 / 0.287 / 0.282, at or below the 0.300 coin-flip line) and `families` trips the negative-control clause at -0.131 and the faithfulness clause at continuity 0.520. The disjunction is doing its job and each space's specific reason is rendered on the page; the problem is the shared headline. `structure`'s neighbourhoods genuinely are ties (actin's median TM among measured pairs is 0.9925, so a k=15 neighbour list is arbitrary) but the same `diagnostics.json` records its partition beating its own control by +0.117 and recovering Pfam family at ARI 0.121, AMI 0.438, z 51.5 against a 200-draw permutation control. "Do not read this protein's neighbours" is true; "this map should not be read as a finding" is not, and the second sentence is the one in 48-point type. **A second cohort now says the complaint generalises and the explanation does not.** A 2530-protein S1 peptidase run at median TM 0.8169 -- far less saturated -- bands the SAME four of five, not fewer, at a matched k=15 and comparable subsample sizes. What changes is *which* space survives, and both survivals turn on a stability difference of under 0.06 straddling a hard threshold: `structure` is 0.322 on chymotrypsin and 0.264 on actin, `physicochemistry` is 0.337 on actin and 0.246 on chymotrypsin. On chymotrypsin the banded-but-informative space is `physicochemistry`, whose partition beats its control by **+0.372** while it is banded unreadable on neighbourhood stability alone -- the same shape as actin's `structure`, on a different space. `families` trips the control clause on both (-0.054, -0.131). | `ProteinCartography/explorer/payload.py` (`space_verdict`) | Deferred, and it remains a wording and granularity decision rather than a defect -- every number is already computed, written and rendered. Candidate, now with two cohorts behind it: let the headline name the scope that failed ("neighbourhoods here are unreliable" against "this partition is no better than chance") rather than share one sentence across clauses that mean different things. **The saturation framing this entry previously proposed is withdrawn**: it predicted the less-saturated cohort would band fewer spaces and it bands the same number, and `physicochemistry`, which has no structural saturation to suffer from, moves in the opposite direction between the two cohorts. Note also that this entry replaced an earlier version claiming `families` was not banded and that the verdict read only neighbourhood stability; both were wrong, and the verdict ORs stability, faithfulness, partition caveats and the control margin. |
| 59 | **The metricity fraction is mostly an artifact of the `1 - TM` transform, and the module's own validity claim is false for the input the pipeline actually supplies.** `blocks/tmscore.py:205` forms `distances = 1.0 - similarity`. By Schoenberg's criterion, for a positive semi-definite `S` with unit diagonal it is `sqrt(2(1 - S))` that is Euclidean; `1 - S` in general is not. Measured on cosine similarities of random unit vectors -- Euclidean by construction, PSD asserted -- `1 - S` returns **41.8% / 42.5% / 43.3% / 43.4%** at n=100/200/400/600 while `sqrt(2(1 - S))` returns ~1e-15. So roughly forty points of every figure this module has ever reported on a `1 - TM` matrix are manufactured by the transform. Consequences: the published **44.6%** for the 160-protein actin cohort is barely above that floor, and the 2530-protein cohort's **31.8%** is *below* what exactly-Euclidean data scores in the same convention. The module docstring claimed "a genuinely Euclidean distance matrix ... returns under 1e-9 here", which is true only for an already-metric distance matrix -- **a shape the pipeline never constructs**. | `ProteinCartography/diagnostics/metricity.py`; `ProteinCartography/blocks/tmscore.py:205` | **The false claims are corrected and a characterization test now pins the artifact (`test_one_minus_similarity_manufactures_negative_mass_on_euclidean_data`); the transform itself is deliberately NOT changed here.** Switching to `sqrt(2(1-S))` would change every manifest carrying the number and is a design decision, not a bugfix -- and the right fix may instead be to report the Cailliez or Lingoes additive constant, which measures how far the matrix is from Euclidean in a way that is invariant to this choice. Note the existing `test_a_euclidean_matrix_has_essentially_no_negative_mass` is correct and was never wrong; it passes a true distance matrix, so it could not catch this. That is the failure mode worth remembering: **a correct test exercising an input shape the production path never produces.** |
| 60 | **The pipeline's "TM-score" is computed from a 3Di+AA alignment, not from TM-align, and nothing says so.** `run_foldseek_clustering` calls `foldseek search <db> <db> <out> <tmp> -a` with no `--alignment-type`, taking foldseek's default (`2: 3Di+AA`), and `aln2tmscore` then computes a TM-score *from that alignment*. Measured with a controlled run on 120 actin structures, varying **only** `--alignment-type` and holding the search path, the output field and every other flag fixed: `1: TM alignment` returns a **median 0.1089 higher** (mean +0.0901, sd 0.1389, max |d| 0.7547), **82.0% of pairs higher and 18.0% lower**, and **96.9% differ by more than 0.01**. So it is neither a constant offset nor a bound in either direction. The gap is largest exactly where thresholds are applied: **+0.1540** median where the shipped score is below 0.30, +0.1151 between 0.30 and 0.70, +0.0396 above 0.70. The 0.17 significance point and the 0.5 same-fold convention are TM-align conventions, and `docs/INTERPRETING.md` and the geometry analysis both discuss them as if the two quantities were interchangeable. **The score-extraction path is NOT implicated and was checked**: `aln2tmscore` + `createtsv` and `easy-search --format-output alntmscore` agree on 100.0% of 13,658 shared pairs, bit for bit, so the whole difference is the alignment. **Separately, that control turned up a second censoring channel**: the shipped call returned 13,658 of 14,280 possible directed pairs at n=120, where `--max-seqs 1000` cannot bite, so **4.4% of pairs are dropped by the default prefilter/E-value alone**. A cohort is not uncensored merely because it is smaller than the cap. | `foldseek_clustering.py:87,90` | Not a bug and not proposed as a change: `--alignment-type 1` runs foldseek's `tmalign` module and is far slower, which is presumably why the default stands. The deferred work is (a) naming the quantity accurately wherever it is reported, and (b) deciding whether the alignment type, the cap and the E-value cutoff should be explicit parameters rather than inherited defaults. Both touch files outside this branch's scope, and (a) changes user-visible text in the manifest and the explorer. |
| 61 | **`min_domain_length` is applied to the declared chopping, never to the residues actually cropped, so a seven-residue fragment can enter the domain cohort as a domain.** TED boundaries are fetched against the *canonical* accession -- `canonical_uniprot_accession` strips the `-2`/`-3` isoform suffix -- but the structure being cropped is the isoform's. Measured on both cohorts on this machine, and the rate is not small: of 2,465 actin structures 25 carry an isoform suffix and **14 of those 25 (56%) have a canonical chopping that runs past the end of the isoform structure**; of 2,526 chymotrypsin structures 110 carry one and **77 of those 110 (70%) do**. Counting domains rather than proteins, 28 of 71 and 95 of 273 crop short of their declared length. `Q80US4-3` is the worst seen, a chopping ending at residue 562 against a 266-residue model. `filter_domain_rows` screens `end - start` *before* the crop, and `assign_and_crop_hits` then rejects the result only when `n_ca <= 0`, so a partial crop passes both. Concrete: `Q9GZN1-2` chopping `177-278` declares 102 residues and crops to **7**, which is written to `domain_features.tsv` with `nres_domain=7` and its PDB written to the domain structures directory. `Q80US4-3` `216-281_441-463` crops to 51 of 89. Only the 0-residue case is caught. Note the 11 isoforms that pass the length test are not thereby correct -- AFDB numbers an isoform model from 1 against the isoform sequence while TED's boundaries index the canonical one, so the mapping can be wrong without running off the end. | `domain_utils.py:223` (`crop_pdb_file`); `assign_domains.py` (`assign_and_crop_hits`, the `n_ca <= 0` guard); `domain_utils.py` (`filter_domain_rows`) | Deferred: it is on `Snakefile_domain`, which this branch does not touch, and the honest fix is a decision rather than a line -- re-apply the floor to `nres_domain` (cheap, drops truncated fragments), or resolve isoform boundaries properly (correct, needs an isoform-aware source TED does not expose). The rate is small on a whole-chain cohort and would matter much more to any domain-level analysis, which is why it is recorded before that work rather than after. |
| 62 | **Neighbourhood stability is measured on the high-dimensional space, but the explorer renders it as a verdict about the map.** `diagnose_space.py:318` passes `high` -- the fused feature matrix -- to `neighborhood_stability`, while `_faithfulness_for` at :306 is handed both `high` and the embedding. So stability answers "were these neighbourhoods ever determinate", which is upstream of any layout, and faithfulness is the only diagnostic that sees the 2-D coordinates. That is a defensible design and the docstring at `diagnostics/stability.py:11` is consistent with it. The problem is downstream: `space_verdict` escalates a space to **unreadable** on `stability[0].informative` and the explorer paints that banner over the *map*, so a reader is told the picture cannot be read on the strength of a number that never looked at the picture. **Measured**: sweeping UMAP `n_neighbors` over {5,10,15,25,40,60,80,120} on two cohorts and two spaces, the reported stability is invariant by construction, and stability recomputed on the 2-D layout returns **0 coin flips at all 32 grid points** -- it does not discriminate between a good layout and a torn one. At nn=5 continuity minima fall to 0.4596, so torn layouts were present in the sweep and stability was silent about every one of them. The converse also holds: a space can be stable in high dimensions and badly embedded, and nothing will say so. | `diagnose_space.py:318`; `explorer/payload.py` (`space_verdict`) | Deferred, and deliberately not fixed here: changing what escalates a space to unreadable changes shipped verdict text on a branch already under review, and the right fix is a decision rather than a line -- either report the two stabilities separately (space-level and layout-level, which is what the geometry analysis asks for when it makes per-point stability a permanent map overlay), or reword the verdict so it does not claim to be about the map. Note the second option is nearly free and the first needs a per-protein series the diagnostic computes and then discards. **Half of this is now done**: `diagnose_space` writes the per-protein series to `spaces/<id>/stability.tsv`, beside the faithfulness file it already wrote, and the explorer's stability panel draws it per space with the coin-flip threshold marked and says in as many words that it judges the space rather than the layout. What remains deferred is the part that is a decision: `space_verdict` still escalates a space to **unreadable** on a stability number that never saw the picture, and changing that changes shipped verdict text on a branch already under review. |
| 63 | **A map's censored twin is less superimposable on it than an entirely different modality is, so the capped-vs-exhaustive drag panel the geometry analysis asks for cannot be drawn as a displacement.** Built to find out: actin_B's 308 redundancy-filtered survivors taken out of the 2,465-protein parent run's capped matrix by label, mask intact, and reduced with the same block, reducer and seed as the shipped exhaustive space -- two layouts of the same proteins differing only in which pairs were measured. Neighbourhood Jaccard at k=15 is **0.137**, cluster ARI **0.078**, and Procrustes disparity **0.968** -- worse than `structure` against `local_structure` (0.862) and worse than `structure` against `fused_late` (0.791). **Controlled against the reducer's own noise**: the same matrix reduced with the same parameters and only a different seed gives 0.010 / 0.031 / 0.025 across three pairs, so the reducer is stable to three decimals on this data and the censoring effect is about thirty times the largest seed effect. Censoring is not reducer noise, and it does not merely move points -- it replaces the frame. Separately measured: censoring among the survivors is **0.825** against **0.595** across the whole parent cohort, so the redundancy filter selects proteins that were *less* measured against each other than average. | `explorer/payload.py` (`_censoring_comparison`); `diagnostics.censoring_comparison` | Not deferred as a defect -- the panel is built and it **refuses** the displacement with the measured disparity as its reason, drawing frame-independent neighbourhood retention instead. What is deferred is the question the number raises: the shipped default pipeline caps every query, so every map this pipeline draws is the censored twin of a map nobody has seen. Whether that is acceptable is a decision about the retrieval stage rather than about the explorer, and it wants a second cohort before anyone generalises from one. |
